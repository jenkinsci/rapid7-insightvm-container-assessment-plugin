<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry field="contains" title="${%Contains}">
        <f:textbox clazz="required" checkMethod="post"/>
    </f:entry>
    <f:entry field="action" title="${%Build.Action}">
        <f:select/>
    </f:entry>
</j:jelly>
